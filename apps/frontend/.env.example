# Public API URL for the Waremax simulation backend.
# Must not have a trailing slash.
# For local development:
PUBLIC_API_URL=http://localhost:8080

# For production (used during Docker build):
# PUBLIC_API_URL=https://waremax-api.skelfresearch.com
